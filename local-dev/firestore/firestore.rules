rules_version = '3';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes for development/emulator
    // In production, replace with proper security rules
    match /{document=**} {
      allow read, write: if request.auth != null;
      allow read, write: if request.app.debug == true;
    }
  }
}

