apiVersion: v1
kind: Service
metadata:
  name: trips-postgres-tenant
  namespace: trip-manager-tenant
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: trips-postgres
    tenant: tenant
