apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: trips-postgres-muster-secret
  namespace: trip-manager-muster
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: gcp-secret-store
  target:
    name: trips-postgres-muster-secret
  data:
    - secretKey: POSTGRES_PASSWORD
      remoteRef:
        key: enterprise-muster-db-password
