apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: trips-postgres-muster
  namespace: trip-manager-muster
spec:
  serviceName: trips-postgres-muster
  replicas: 1
  selector:
    matchLabels:
      app: trips-postgres
      tenant: muster
  template:
    metadata:
      labels:
        app: trips-postgres
        tenant: muster
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          args: ["-c", "hba_file=/etc/postgresql/pg_hba.conf"]
          env:
            - name: POSTGRES_DB
              value: trips
            - name: POSTGRES_USER
              value: trips_enterprise
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: trips-postgres-muster-secret
                  key: POSTGRES_PASSWORD
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
              subPath: postgres
            - name: config
              mountPath: /etc/postgresql
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: config
          configMap:
            name: trips-postgres-muster-config
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: standard-rwo
        resources:
          requests:
            storage: 5Gi
