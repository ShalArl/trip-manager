apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: trips-postgres-test-secret
  namespace: trip-manager-test
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: gcp-secret-store
  target:
    name: trips-postgres-test-secret
  data:
    - secretKey: POSTGRES_PASSWORD
      remoteRef:
        key: enterprise-test-db-password
