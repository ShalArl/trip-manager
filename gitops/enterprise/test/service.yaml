apiVersion: v1
kind: Service
metadata:
  name: trips-postgres-test
  namespace: trip-manager-test
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: trips-postgres
    tenant: test
