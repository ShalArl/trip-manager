---
# Source: locations-postgres/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: locations-postgres
  namespace: trip-manager-prod
spec:
  selector:
    app: locations-postgres
  ports:
    - port: 5432
      targetPort: 5432
---
# Source: locations-postgres/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: locations-postgres-headless
  namespace: trip-manager-prod
spec:
  clusterIP: None   # Headless
  selector:
    app: locations-postgres
  ports:
    - port: 5432
      targetPort: 5432
