---
# Source: trips/templates/externalsecrets.yaml
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: trips-secrets
  namespace: trip-manager-prod
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: trips-secrets
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: trips-database-url

