{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/api",
    "build": "go build -o bin/api ./cmd/api",
    "lint": "golangci-lint run ./...",
    "test": "go test ./...",
    "gen": "cd .. && pnpm gen:go"
  }
}
