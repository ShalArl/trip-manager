apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app:  minio
  ports:
    - name: api
      port: {{ .Values.service.apiPort }}
      targetPort: api
    - name: console
      port: {{ .Values.service.consolePort }}
      targetPort: console