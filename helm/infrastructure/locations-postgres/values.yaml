image:
  repository: postgres
  tag: "16-alpine"

replicaCount: 1

postgres:
  db: locations
  user: locations_user
  password: locations_password

service:
  port: 5432

persistence:
  enabled: false
  size: 1Gi
  storageClass: ""

externalSecrets:
  enabled: false