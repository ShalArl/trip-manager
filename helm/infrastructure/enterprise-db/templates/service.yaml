apiVersion: v1
kind: Service
metadata:
  name: trips-postgres-{{ .Values.tenantSlug }}
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: trips-postgres
    tenant: {{ .Values.tenantSlug }}