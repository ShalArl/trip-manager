apiVersion: v1
kind: Service
metadata:
  name: trips-postgres
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: trips-postgres
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5432

---

apiVersion: v1
kind: Service
metadata:
  name: trips-postgres-headless
  namespace: {{ .Release.Namespace }}
spec:
  clusterIP: None   # Headless
  selector:
    app: trips-postgres
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5432